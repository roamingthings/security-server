include 'auth-server', 'usermanagement-server'

def persistenceSupportPath = "${rootProject.projectDir}/../../commons/persistence-support"
if (new File(persistenceSupportPath).exists()) {
    includeBuild persistenceSupportPath
}